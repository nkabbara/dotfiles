return {
  opencode = require("custom.workflow.opencode"),
  workspace = require("custom.workflow.workspace"),
  worktree = require("custom.workflow.worktree"),
  win_manager = require("custom.workflow.win_manager"),
}
