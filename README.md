# Kanata

To setup kanata on a new computer:
1. `brew install kanata`
2. `brew install kanata-tray`
3. Run `nohup sudo KANATA_TRAY_CONFIG_DIR=$KANATA_TRAY_CONFIG_DIR kanata-tray &`

This assumes that you've chezmoi applied the relevant configs.
